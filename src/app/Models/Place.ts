export interface Place {
    name: String,
    kind_of_place: String,
    area: String,
    description: String,
    score_avg: Number,
    wifi: Boolean,
    handicap: Boolean,
    swimming_pool: Boolean,
    animal: Boolean,
    shaabaat_keep: Boolean,
    price_night: Number,
}